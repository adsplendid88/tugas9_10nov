name: tugas9_10nov
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  dependencies:
    flutter:
      sdk: flutter
    http: ^1.1.0
    dependencies:
      flutter:
        sdk: flutter
      provider: ^6.1.1 # atau versi terbaru saat ini
      dependencies:
        flutter:
          sdk: flutter
        http: ^1.2.0  # ← Tambahkan ini